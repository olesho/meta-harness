# Copy this file to your repo root as `sonar-project.properties` and fill in the key/name.
# sonar.projectKey must be unique across the shared SonarQube instance — use the repo name.
sonar.projectKey=meta-harness
sonar.projectName=meta-harness
sonar.sources=.
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/*.min.js,**/vendor/**
sonar.sourceEncoding=UTF-8
