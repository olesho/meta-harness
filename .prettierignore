node_modules/
dist/
coverage/
pnpm-lock.yaml

# veracity-managed wiring: rendered by the veracity binary (Go json.MarshalIndent,
# which formats arrays differently from prettier). veracity owns and re-renders
# these on every hook run, so prettier must not fight its output.
.claude/settings.json
veracity.lock.json

# Generated by `veracity docs render` — re-emitted unformatted each run.
docs/modules.html

# Vendored cross-language conformance corpora (wire + auth): mirrored byte-for-byte
# from harness-wrapper and frozen by each corpus's MANIFEST.sha256, which is exactly
# what `scripts/sync-corpus.sh --check` verifies. Reformatting a fixture would both
# stale the manifest and diverge our copy from the canonical bytes it exists to mirror
# — see test/corpus/wire/README.md ("Do not hand-edit fixtures here").
test/corpus/

# Same rule, different corpus: the vendored cross-language CONFORMANCE corpus,
# mirrored by `scripts/sync-conformance.sh` and frozen by a MANIFEST.sha256 that
# hashes every corpus `*.json`. Reformatting a fixture would stale that manifest
# and break parity with harness-wrapper's scripts/check-conformance-corpus.sh.
# README.md is unhashed but is regenerated by the sync script, so it is ignored
# too rather than being reformatted back and forth on every pull.
test/conformance/
