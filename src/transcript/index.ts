// Public barrel for `meta-harness/transcript`.
//
// Stub for Phase 0 — concrete exports land in later phases. This barrel must
// only ever re-export from src/transcript/** (never from src/internal/**).
export {}
