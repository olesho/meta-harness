// Public barrel for `meta-harness/chat`.
//
// Stub for Phase 0 — concrete exports land in later phases. This barrel must
// only ever re-export from src/chat/** (never from src/internal/**).
export {}
