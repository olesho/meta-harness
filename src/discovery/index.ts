// Public barrel for `meta-harness/discovery`.
//
// Stub for Phase 0 — concrete exports land in later phases. This barrel must
// only ever re-export from src/discovery/** (never from src/internal/**).
export {}
